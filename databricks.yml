bundle:
  name: snowflake_databricks_poc

include:
  - resources/*.yml

variables:
  catalog:
    description: Unity Catalog
    default: uc_dev_snt_fdn

targets:
  dev:
    mode: development

    workspace:
      host: https://dbc-fa0d2b9b-7734.cloud.databricks.com
      root_path: /Workspace/Shared/.bundle/${bundle.name}/${bundle.target}

    variables:
      catalog: uc_dev_snt_fdn